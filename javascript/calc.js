function addition(){
    let a=Number(prompt("enter the first digit"));
let b=Number(prompt("enter the second digit"));
let c=Number(prompt("enter the third digit"));
document.write("addition =" +(a+b+c));
}

function subraction(){
    let a=Number(prompt("enter the first digit"));
    let b=Number(prompt("enetr the second digit"));
    let c=Number(prompt("enter the third digit"));
    document.write("subration = " + (a-b-c));
}
function division(){
    let a=Number(prompt("enter the first digit"));
    let b=Number(prompt("enetr the second digit"));
    let c=Number(prompt("enter the third digit"));
    document.write("division = " + (a/b/c));
}
function multiplication(){
    let a=Number(prompt("enter the first digit"));
    let b=Number(prompt("enetr the second digit"));
    let c=Number(prompt("enter the third digit"));
    document.write("multiplication = " + (a*b*c))
}
function modulo(){
    let a=Number(prompt("enter the first digit"));
    let b=Number(prompt("enetr the second digit"));
    let c=Number(prompt("enter the third digit"));
    document.write("modulo= " + (a%b%c));
}